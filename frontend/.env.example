# Local dev: use Vite proxy (/api → localhost:3001)
# Production (Vercel): set to your Render/Railway backend URL
VITE_API_URL=/api

# Enable API request logs in production (debug Overview blank screen)
# VITE_LOG_API=true
